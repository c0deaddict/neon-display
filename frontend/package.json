{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend for neon-display",
  "main": "app.jsx",
  "scripts": {
    "build": "node scripts/build.mjs",
    "minify": "npm run build -- --minify",
    "serve": "npm run build -- --serve --watch",
    "watch": "npm run build -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jos van Bakel",
  "license": "MIT",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
